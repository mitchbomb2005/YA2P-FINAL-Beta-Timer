'use strict';

// rename this to Player.js

export class Camera {

    x;
    y;
    constructor(x, y, debug, ) {
        this.x = x
        this.y = y
    }

    update() {
        if (true) {}
    }

    freeCamMove( ) {}
    
}
