'use strict';

// rename this to Player.js

export class Camera {

    x;
    y;
    debug;

    constructor(x, y, debug) {
        this.x = x
        this.y = y
        this.debug = debug
    }

    update() {
        if (true) {
            
        }
    }

    freeCamMove( ) {}
    
}
